"use strict";
//storing the quote and its author name
let quote = "Change. But start slowly, because direction is more important than speed.";
let author = "Paulo Ceolho";
//printing the message
console.log(`"${quote}" - ${author}`);