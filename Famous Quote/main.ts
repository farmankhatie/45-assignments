//storing the quote and its author name
let quote:string = "Change. But start slowly, because direction is more important than speed."

let author:string = "Paulo Ceolho"
//printing the message
console.log(`"${quote}" - ${author}`)